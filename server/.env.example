# Server port (default: 4321)
PORT=4321

# Server password (optional, clients must provide this to connect)
# SERVER_PASSWORD=your_shared_secret_here

# TLS: provide both to enable HTTPS
# TLS_CERT=/path/to/cert.pem
# TLS_KEY=/path/to/key.pem

# GitHub OAuth (required when REQUIRE_GITHUB_AUTH=true)
# REQUIRE_GITHUB_AUTH=true
# GITHUB_CLIENT_ID=your_github_oauth_app_client_id
# GITHUB_CLIENT_SECRET=your_github_oauth_app_client_secret

# JWT secret for signing auth tokens (required when REQUIRE_GITHUB_AUTH=true)
# Must be a strong random value in production.
# JWT_SECRET=your_random_secret_here

# CORS allowed origin (default: "*" allows all origins)
# Set to your domain in production, e.g. https://yourdomain.com
# CORS_ORIGIN=*
